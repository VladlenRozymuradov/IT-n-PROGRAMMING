#include <iostream>

using namespace std;

template <typename T> void createnreturn(T *mas, int n){
    for (int i=0; i<n; i++) cin>>*(mas+i);
}

int main()
{
    int n;
    cin>>n;
    int *mas=new int(n);
    createnreturn(mas, n);
    cout<<*(mas+2);
    return 0;
}
